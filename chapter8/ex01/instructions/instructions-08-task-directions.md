## Task 08

Add two `fieldset`s within the `form` element that you created in Task 07.

Include a `legend` for each `fieldset`. Use the text **Customer Information** for the first `legend`, and use the text **Musical Interest** for the second `legend`.

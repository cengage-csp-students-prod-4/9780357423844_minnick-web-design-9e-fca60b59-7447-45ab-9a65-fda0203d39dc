## Task 06

In the _styles.css_ file, find the desktop media query.

Below the last style rule of this media query, add a new comment with the text **Desktop Viewport: Style rules for table element**.

Below the comment, add a style rule for the `table` selector that sets the width to **70%**.

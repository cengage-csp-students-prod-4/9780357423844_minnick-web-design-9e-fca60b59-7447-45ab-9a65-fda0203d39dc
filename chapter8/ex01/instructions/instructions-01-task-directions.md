## Task 01
Open the file  *template.html* and use it to create a new page with the file name **rentals.html**.

Review the contents of the html in *rentals.html*.

Update the comment with **your name** (Firstname Lastname), the **file name** and **today’s date** (MM/DD/YYYY). 

In the `title` element, replace the word *Template* with **Rentals**. 

Add an `id` attribute with a value of **rental** to the empty `div` element nested within the `main` element.

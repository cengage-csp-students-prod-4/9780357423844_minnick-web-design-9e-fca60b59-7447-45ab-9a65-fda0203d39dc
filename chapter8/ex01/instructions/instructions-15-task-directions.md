## Task 15

In _styles.css_, within the tablet media query, below the `.map` style rule, add a new comment with the text **Tablet Viewport: Style rule for form element**

Below this comment, create a style rule for the `form` element that:

- sets the width to **70%**
- sets the top and bottom margin to **0px** and left and right margin to **auto**

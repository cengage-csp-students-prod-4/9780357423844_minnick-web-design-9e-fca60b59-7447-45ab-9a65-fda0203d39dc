## Task 04
In the _styles.css_ file, locate the mobile style rule for `#contact`. 

Add the `id` for `#rental` and `#form` as additional selectors for this style rule.

> Note: The `#form` styles will be tested later in this lab when the `h2` element is added to the _contact.html_ file. 
 

## Task 02
Review the `form` elements within the html of the _index.html_ file. 

Add a `label` element for each of the html form's `input` elements. 

Be sure to include a properly formatted `for` attribute to each of your `labels`. 

## Task 03

Review _Figure 8–69_ and _Figure 8–70_. Take note of where **Customer Information** and **Payment Information** `fieldset`s are displayed in the images of the completed html form. These `fieldset`s are used show a logical group of `form` elements within the `form`.

Analyze the html elements in the `form` in _index.html_ to determine where the `fieldset`s should start and stop.

Within the `form` in _index.html_ add a **Customer Information** `fieldset` element and a **Payment Information** `fieldset`, each containing the appropriate `form` elements.

## Task 08
In the *index.html* file, add an **onclick** event handler to the first `img` element that calls the `pic1()` function. Add an **onclick** event handler to the second `img` element that calls the `pic2()` function. Add an **onclick** event handler to the third `img` element that calls the `pic3()` function.
 